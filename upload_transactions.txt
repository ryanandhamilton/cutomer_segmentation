COPY INTO "CUSTOMER_DATA"."TRANSACTIONS"."TRANSACTIONS"
FROM '@"CUSTOMER_DATA"."TRANSACTIONS"."%TRANSACTIONS"/__snowflake_temp_import_files__/data.csv'
FILE_FORMAT = (
    TYPE=CSV,
    SKIP_HEADER=1,
    FIELD_DELIMITER=',',
    TRIM_SPACE=FALSE,
    FIELD_OPTIONALLY_ENCLOSED_BY='"',
    DATE_FORMAT=AUTO,
    TIME_FORMAT=AUTO,
    TIMESTAMP_FORMAT='MM/DD/YYYY HH24:MI',
    ENCODING = "iso-8859-1"
)
ON_ERROR=ABORT_STATEMENT
PURGE=TRUE